{% extends 'base.html.twig' %}

{% block body %}
    <div class="card shadow mx-auto" style="width: 80%;">
        <div class="p-3 m-0">
            <h1 class="text-center">Modifier un livre</h1>
            {{ include('books/_form.html.twig', {'button_label': 'Valider'}) }}
            <a class="btn" href="{{ path('app_books_index') }}">Retour</a>
            {% if is_granted('ROLE_ADMIN') %}
                {{ include('books/_delete_form.html.twig') }}
            {% endif %}
        </div>
    </div>
{% endblock %}
